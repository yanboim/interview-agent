# 技术债跟踪器

本文件是按优先级排列的架构技术债登记表。普通产品缺陷应留在常规Issue跟踪器中，
除非它代表重复出现的系统边界问题。

| ID | 优先级 | 区域 | 问题 | 退出标准 | 状态 |
|---|---|---|---|---|---|
| TD-001 | P0 | 知识 | 新集合通过回归评估前，导入会删除正在服务的Qdrant集合。 | 构建版本化集合、验证、原子切换别名、版本化/失效缓存并保留回滚版本。 | 2026-07-26完成 |
| TD-002 | P0 | 面试 | 并发或重试提交答案可能对同一回合重复评分或推进。 | 具备持久回合身份、幂等键、条件状态转换和并发测试。 | 2026-07-26完成 |
| TD-003 | P1 | 聊天 | 聊天在生成前写入用户消息，缺少持久的逐回合生命周期。 | 测试pending/generating/completed/failed状态、重试语义、会话排序和断连恢复。 | 2026-07-26完成 |
| TD-004 | P1 | API架构 | `app/main.py` 混合传输、DTO、编排、管理、文件和生命周期初始化，且超过2,000行。 | 领域路由调用应用服务；组合根只包含装配和中间件，不包含业务流程。 | 2026-07-26完成 |
| TD-005 | P1 | 持久化 | 同步SQLAlchemy、分散的 `asyncio.to_thread` 和进程锁使事务/并发行为不清晰。 | 采用一种有文档的同步或异步数据库模型，明确请求/用例事务所有权，正确性不依赖进程锁。 | 2026-07-26完成 |
| TD-006 | P1 | 任务 | Redis `BLPOP` 任务没有确认、重试、租约、死信或幂等语义。 | 建立持久领取/确认/重试流程和崩溃恢复测试。 | 2026-07-26完成 |
| TD-007 | P1 | LLM | 提供方调用缺少统一的超时、重试、并发、预算和错误策略网关。 | 所有模型调用使用统一策略网关，并暴露延迟、Token和错误指标。 | 2026-07-26完成 |
| TD-008 | P2 | 上下文 | 聊天向模型发送无界会话历史。 | 使用Token预算的近期历史、持久摘要和截断测试。 | 2026-07-26完成 |
| TD-009 | P2 | 开发环境 | 固定Compose名称、端口和共享资源名阻止每个Worktree独立运行。 | 生成项目/资源后缀，使两个Worktree可并行运行。 | 2026-07-26完成 |
| TD-010 | P2 | 可复现性 | 宽泛Python最低版本和 `latest` Qdrant镜像导致环境不可复现。 | 在CI中强制经过评审的Lock更新流程和不可变生产镜像版本。 | 2026-07-26完成 |
| TD-011 | P2 | 前端工具链 | 开发依赖审计仍报告6个Vite/vue-tsc相关警告；这些包只用于构建，不进入生产运行依赖。 | 在一次评审变更中升级相关主版本、更新Lockfile，并在无这6个警告时通过 `make harness-check`。 | 2026-07-26完成 |
| TD-012 | P2 | 运维 | Worker没有持久心跳或新鲜度信号，资源中心只能盘点它，运行状态为未知。 | Worker发布有界生命周期心跳，资源中心通过崩溃/重启测试验证新鲜度。 | 2026-07-28完成 |
| TD-013 | P1 | 运维 | 管理员无法权威回答谁执行了动作、用户收到什么输入/输出、Agent/工具如何形成结果。 | 具备服务端活动审计、权威跨用户交互检查、请求关联执行追踪、管理员读取审计、迁移覆盖和管理UI验证。 | 2026-07-28完成 |
| TD-014 | P0 | Agent安全与隐私 | 工具审计摘要可能复制内容型输入/结果，公开搜索DLP范围较窄，不可信检索内容缺少明确的注入边界，模型选择的变更操作缺少工具级确认协议。 | 审计只保留安全元数据；外发查询通过已测试的DLP；检索内容按不可信证据处理；所有变更操作使用绑定所有者与内容的预览和确认。 | 已于2026-07-31完成；通过 `agent-tool-safety-boundary` 契约和 `make harness-check` 验证 |
| TD-015 | P1 | Agent质量 | 回答/来源元数据只到回合级，结构化结果依赖自由文本JSON提取，评测规模较小或只检查元数据，浏览器反馈没有进入持久质量闭环。 | 具备声明级证据、已验证的结构化输出、真实Agent栈分组评测门禁，以及带评测候选审核入口的用户级回合反馈。 | 已于2026-07-31完成；由 `agent-quality-feedback-learning-loop` 契约及230例确定性Agent门禁验证 |
| TD-016 | P1 | Agent编排与上下文 | Specialist委派和聊天尚未形成完整的个性化、持久、可恢复Agent工作流，也缺少可检查的记忆和命令状态。 | 验证受预算约束的服务端上下文快照、经确认的教练记忆、版本化委派信封，以及至少一个持久且幂等的多步骤训练工作流。 | 2026-07-31完成；由 `agent-personalized-context-memory` 和 `durable-personalized-training-workflow` 契约验证 |
| TD-017 | P2 | Agent效率与学习 | 强制使用同一模型委派会增加成本/延迟，能力聚合校准较弱，复习间隔不考虑回忆结果。 | 验证按用途的模型路由/预算、经过评测的回退策略、感知置信度的能力聚合，以及感知结果的确定性复习调度。 | 已于2026-07-31完成；由 `agent-quality-feedback-learning-loop` 和 `agent-model-cost-resilience-routing` 验证 |
| TD-018 | P1 | Harness安全与可观测性 | 未知的就绪、面试、队列和知识异常可能进入公开或管理员响应，而核心业务指标只有进程本地兼容视图。 | 使用包含模拟秘密的异常验证稳定公开错误；核心指标同时具备OTLP导出路径，随附的Collector可接收Metrics。 | 已于2026-08-01完成；由 `model-policy-gateway`、`admin-observability-and-audit` 和完整Harness组件门禁验证 |
| TD-019 | P1 | Agent编排 | 兼容Supervisor增加嵌套模型调用，并留下两套竞争的生产拓扑。 | 失败关闭的预发布验收绑定确定性与隔离真实证据、精确身份清理、不可变回滚产物、所有者批准，并移除所有运行时Supervisor选择器、Prompt和工具路径。 | 已于2026-08-02完成；由 `workflow-v2-prerelease-acceptance.json`、`make workflow-prerelease-retirement-check`、聚焦拓扑测试和不可变镜像回滚指南验证 |

2026-07-29 评审说明：简历中心 UI 优化未引入新的架构技术债；其样式按路由
加载，现有模型、持久化和权限边界均未改变。

2026-08-03 评审说明：零chunk SSE重启修复补齐了已完成TD-007网关边界的运行时缺口。
重启仍受模型调用、Token、成本和墙钟预算约束，且不会重放部分流；未引入新的架构技术债。

2026-08-03 评审说明：分层验证移除了Pull Request中重复的浏览器、依赖审计和
镜像工作，同时保留Main和发布的完整门禁。工作流拆分未引入运行时依赖或产品技术债。
