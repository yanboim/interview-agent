# RAG（检索增强生成）

RAG 将外部知识检索与大模型生成结合，典型链路是：文档加载与清洗、分块、Embedding、写入向量数据库、问题向量化、召回、可选重排、上下文拼接、生成答案。

## 分块策略

分块需要平衡语义完整性和检索精度。块太小会丢失上下文，块太大则引入噪声并占用上下文窗口。应根据文档结构设置分隔符，并通过检索命中率和最终答案质量评估 chunk size 与 overlap。

## 评估

检索侧可关注 Recall@K、MRR、命中率；生成侧可关注忠实度、答案相关性和引用准确性。生产系统通常还需记录查询、召回文档、耗时和 token 消耗。
