# 贡献指南

## 开始前

1. 阅读 `AGENTS.md`、`ARCHITECTURE.md` 和相关产品契约。
2. 检查活动执行计划、技术债和当前工作区修改。
3. 明确预期行为、非目标和最小验收条件。
4. 非简单变更在 `docs/exec-plans/active/` 建立计划。

环境准备和命令见[开发指南](docs/development.md)。

## 变更原则

- 一个变更只解决一个清晰问题，不混入无关清理。
- 保留用户和其他开发者已有的未提交修改。
- 新业务逻辑进入应用服务或纯领域模块，不进入组合根。
- 数据库结构变化必须包含Alembic迁移和迁移测试。
- 用户资源读写必须使用服务端认证身份。
- 外部调用必须有超时、安全错误、指标和预算行为。
- 代码、测试、产品契约和文档在同一变更中更新。

## 提交与评审

- 使用说明意图的提交信息，例如 `docs: add product and lifecycle baseline`。
- PR描述包括背景、范围、风险、验证、迁移和回滚。
- 不提交 `.env`、Token、密钥、数据库Dump、备份、用户数据或私人知识摘录。
- 评审按[代码评审清单](docs/sdlc/CODE-REVIEW.md)执行。

## 验证

迭代时运行聚焦测试，仓库级变化运行：

```bash
make harness-check
```

只改文档且没有运行时依赖时，至少运行：

```bash
make harness-static
```

无法运行要求门禁时，说明确切缺失依赖和实际执行的检查。不要把未运行的检查写成
通过。

## 完成

只有满足[完成定义](docs/sdlc/DEFINITION-OF-DONE.md)后才完成工作；更新执行计划的
验证证据并移入 `completed/`。
